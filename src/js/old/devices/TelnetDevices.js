import React from "react";

import {
	Layout
} from "../layout";

export default class extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
			<Layout>
			</Layout>
        )
    }
}